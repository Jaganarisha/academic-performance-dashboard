# Academic Performance Dashboard
Run in VS Code:
1. `python -m pip install -r requirements.txt`
2. `python -m streamlit run app.py`
If Python command is unavailable, use `py -m pip ...` and `py -m streamlit ...`.
